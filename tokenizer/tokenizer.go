package tokenizer

// func Tokenize(app *app.App, id int) ([]string, error) {
// 	db.GetSnippet(id)
// 	i = strings.ToLower(i)

// 	tR, err := regexp.Compile("[^a-z0-9_'-]+")
// 	if err != nil {
// 		return nil, errors.New("failed to compile the tokenizer regular expression")
// 	}

// 	return tR.Split(i, -1), nil
// }
